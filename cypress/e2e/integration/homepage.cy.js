describe('homepage', () => {
  it('user should visit our app', () => {
    // visit homepage
    cy.visit('/')
    // see menu with logo, items and icons
    // see logo
  });

  // it('user should see menu with logo, items and icons', () => {
  //   cy.visit('/navigation')

  // })
})