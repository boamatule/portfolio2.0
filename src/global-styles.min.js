import styled,{createGlobalStyle}from"styled-components/macro";export const GlobalStyles=createGlobalStyle`
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }
  
  html {
    box-sizing: border-box;
    font-family: 'IBM Plex Mono', monospace;
    background-color: #000000;
  }
  
  a {
    text-decoration: none;
    color: black;
  } 
`;export const Container=styled.div`
  max-width: 1000px;
  margin: 0 auto;
  padding: 0 4px;
`;export const Button=styled.button`
  white-space: nowrap;
  padding: ${({big:big})=>big?"12px 4px":"10px 20px"};
  color: grey;
  font-size: ${({fontBig:fontBig})=>fontBig?"20px":"16px"};
  outline: none;
  cursor: pointer;
  background: #4568dc;
  /* background: #4568dc; */

  transition: 0.5s;
  border-radius: 2px;

  &:hover {
    transition: all 0.3s ease-out;
    background: ${({primary:primary})=>"purple"};
    color: grey;
    text-decoration: none;
  }
`;export const Span=styled.span`
  font-weight: normal;
  font-size: 1rem;
  line-height: 20px;
  padding-left: 0px;
  text-align: center;
  background: transparent;
  font-size: 12px;
  text-transform: uppercase;
`;export const Glassmorphism=styled.div`
  background: transparent;
  box-shadow: none;
  backdrop-filter: blur(1px);
  -webkit-backdrop-filter: blur(1px);
  border-radius: 10px 10px 0px 0px;
  border: 1px solid rgba(255, 255, 255, 0.18);
`;